["SELECT * FROM products", "SELECT product_id, name FROM products", "SELECT COUNT(*) FROM products", "SELECT name, category FROM products WHERE stock > 0", "SELECT product_id, name FROM products WHERE price > 100", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 3", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.name", "SELECT p.name, MAX(r.rating) FROM products p JOIN reviews r ON p.product_id = r.product_id GROUP BY p.name", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id ORDER BY oi.quantity DESC", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id ORDER BY r.comment", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity BETWEEN 1 AND 5", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating BETWEEN 2 AND 4", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 GROUP BY p.name", "SELECT p.name, MAX(r.rating) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 GROUP BY p.name", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 ORDER BY oi.quantity DESC", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 ORDER BY r.comment", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity BETWEEN 1 AND 5 AND oi.price > 50", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating BETWEEN 2 AND 4 AND r.comment LIKE '%great%'", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 AND oi.price > 50 GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 AND r.comment LIKE '%great%' GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 AND oi.price > 50 GROUP BY p.name", "SELECT p.name, MAX(r.rating) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 AND r.comment LIKE '%great%' GROUP BY p.name", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 AND oi.price > 50 ORDER BY oi.quantity DESC", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 AND r.comment LIKE '%great%' ORDER BY r.comment", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity BETWEEN 1 AND 5 AND oi.price > 50 AND oi.quantity > 1", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating BETWEEN 2 AND 4 AND r.comment LIKE '%great%' AND r.rating > 4", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 AND oi.price > 50 AND oi.quantity > 1 GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating > 4 AND r.comment LIKE '%great%' AND r.rating > 4 GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity > 1 AND oi.price > 50 AND oi.quantity > 1 GROUP BY p.name", "SELECT category, COUNT(product_id) AS product_count FROM products GROUP BY category", "SELECT p.name, SUM(oi.quantity) AS total_quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) AS review_count FROM products p JOIN reviews r ON p.product_id = r.product_id GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) AS min_quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id GROUP BY p.name", "SELECT p.name, MAX(r.rating) AS max_rating FROM products p JOIN reviews r ON p.product_id = r.product_id GROUP BY p.name", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id ORDER BY oi.quantity ASC", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id ORDER BY r.comment DESC", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity < 5", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating < 2", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity < 5 GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating < 2 GROUP BY p.name", "SELECT p.name, MIN(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity < 5 GROUP BY p.name", "SELECT p.name, MAX(r.rating) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating < 2 GROUP BY p.name", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity < 5 ORDER BY oi.quantity ASC", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating < 2 ORDER BY r.comment DESC", "SELECT p.name, oi.quantity FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity BETWEEN 1 AND 3", "SELECT p.name, r.comment FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating BETWEEN 1 AND 3", "SELECT p.name, SUM(oi.quantity) FROM products p JOIN order_items oi ON p.product_id = oi.product_id WHERE oi.quantity < 5 AND oi.price < 50 GROUP BY p.name", "SELECT p.name, COUNT(r.review_id) FROM products p JOIN reviews r ON p.product_id = r.product_id WHERE r.rating < 2 AND r.comment LIKE '%poor%' GROUP BY p.name"]